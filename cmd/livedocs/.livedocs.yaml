# livedocs configuration
# All fields are optional. An empty file uses sane defaults.
# See: https://github.com/sjarmak/livedocs
languages:
    - go
tribal:
    llm_enabled: true
    daily_budget: 50
